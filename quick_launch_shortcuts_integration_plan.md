# ⚡ 빠른 실행(Quick Launch) & 파일/폴더(Shortcuts) 통합 분석 및 상세 계획서

본 문서는 Util-Tools의 **⚡ 빠른 실행(Quick Launch)**과 **📁 파일 / 폴더(Folder Shortcuts)** 기능의 성격 중복을 해소하고, 단일 **`⚡ 빠른 실행 & 바로가기 (Quick Access Hub)`**로 통합 구성하기 위한 아키텍처 및 UI 설계 계획서입니다.

---

## 1. 문제 진단 및 성격 중복 분석 (Why Integrate?)

### 1-1. 두 기능의 본질적 일치성
현재 두 기능은 별도의 상단 메인 탭으로 분리되어 있으나, 사용 목적은 **"자주 찾는 리소스를 원클릭으로 열거나 실행한다(Launcher / Shortcut)"**는 점에서 본질적으로 100% 동일합니다:

| 구분 | ⚡ 빠른 실행 (`quick_launch`) | 📁 파일 / 폴더 (`shortcuts`) | 본질 |
| :--- | :--- | :--- | :--- |
| **대상 리소스** | 실행 파일(`.exe`, `.bat`), CMD, PowerShell, 웹 URL, SSH | 작업 디렉토리, 로컬 드라이브, 프로젝트 폴더 | **외부 실행/접근 바로가기** |
| **실행 방식** | 프로세스 실행(`subprocess`), 브라우저 오픈(`webbrowser`) | 탐색기 열기(`os.startfile`), 터미널/VS Code 실행 | **시스템 프로세스 호출** |
| **UI 형태** | 카드형 그리드 + [⚙️ 편집] 팝업 + 순서 드래그 | 카드형 그리드 + [⚙️ 편집] 팝업 + 터미널 셀렉트 | **동일한 카드 그리드 UI** |
| **데이터 구조** | `quick_launch` (id, title, path, icon, category) | `shortcuts` (id, title, url_or_path, icon, category) | **거의 동일한 스키마** |

### 1-2. 현재 분리 구조의 사용자 경험(UX) 단점
1. **탐색 동선 단절**:
   - 특정 프로젝트를 작업할 때: "폴더를 열거나 터미널을 띄우려면 `📁 파일/폴더` 탭으로 가고, 메모장이나 계산기나 관련 관리자 툴을 켤 때는 `⚡ 빠른 실행` 탭으로 이동"해야 하는 불필요한 탭 전환 발생.
2. **상단 탭 공간 낭비**:
   - 상단 내비게이션 바에 유사한 성격의 탭 버튼 2개가 나란히 차지하여 공간 효율이 저하됨.

---

## 2. 통합 아키텍처 및 UI 디자인 옵션

### 📌 옵션 A (적극 권장): 상·하 듀얼 섹션 단일 탭 통합 (Dual-Section Hub)
상단 탭을 **`⚡ 빠른 실행` (또는 `⚡ 바로가기`)** 단일 탭으로 합치고, 화면 내부를 직관적인 2개 그룹(폴더 바로가기 + 프로그램 실행)으로 상/하 분할 배치하는 방식입니다.

#### [화면 레이아웃 구조]
```text
┌───────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ ⚡ 빠른 실행 & 바로가기 (Quick Access)                                                                 │
│                                                                                                       │
│ ┌─ 📁 폴더 바로가기 & 터미널 ────────────────────────────────────────── [⚙️ 폴더 편집] [터미널: PS|CMD|Code] ┐ │
│ │  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐                                              │ │
│ │  │ 📂 프로젝트 A   │ │ 💽 C:\ 작업공간 │ │ 📁 공유 드라이브  │                                              │ │
│ │  └───────────────┘ └───────────────┘ └───────────────┘                                              │ │
│ └─────────────────────────────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                                       │
│ ┌─ ⚡ 프로그램 & 도구 실행 ───────────────────────────────────────────── [⚙️ 앱 편집] ─────────────────┐ │
│ │  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐ ┌───────────────┐                            │ │
│ │  │ 🔢 계산기      │ │ 📝 메모장     │ │ 📊 작업 관리자  │ │ 💻 CMD 콘솔   │                            │ │
│ │  └───────────────┘ └───────────────┘ └───────────────┘ └───────────────┘                            │ │
│ └─────────────────────────────────────────────────────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────────────────────────────────────────────────┘
```
- **장점**:
  - 기존의 강력한 **폴더별 빠른 터미널/VS Code 원클릭 실행 바**를 완벽히 유지.
  - 사용자가 작업에 필요한 '폴더'와 '프로그램/웹/SSH'를 한 화면에서 한눈에 조망하고 원클릭 실행.
  - 상단 메인 탭 개수가 1개 줄어들어 내비게이션이 극도로 간결해짐.

---

### 📌 옵션 B: 올인원 통합 그리드 + 카테고리 칩 필터 (Unified Filter Grid)
폴더, 실행 프로그램, URL, SSH를 하나의 통합 카드 목록으로 렌더링하고, 상단에 카테고리 필터 칩을 두는 방식입니다.

#### [화면 레이아웃 구조]
```text
┌───────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ ⚡ 빠른 실행 (Quick Access)                                                                            │
│                                                                                                       │
│ [🔘 전체 (10)]   [📁 폴더 바로가기 (3)]   [💻 애플리케이션 (5)]   [🌐 웹/URL (1)]   [🔒 SSH (1)]   [⚙️ 편집] │
│                                                                                                       │
│ ┌───────────────┐ ┌───────────────┐ ┌───────────────┐ ┌───────────────┐ ┌───────────────┐             │
│ │ 📂 프로젝트 A   │ │ 🔢 계산기     │ │ 📝 메모장     │ │ 💽 C:\ 드라이브 │ │ 🔒 SSH 원격   │             │
│ └───────────────┘ └───────────────┘ └───────────────┘ └───────────────┘ └───────────────┘             │
└───────────────────────────────────────────────────────────────────────────────────────────────────────┘
```
- **장점**:
  - 카드들이 동일한 그리드에 정렬되어 컴팩트함.
- **단점**:
  - 폴더 전용 기능(터미널 셀렉트바 등)이 일반 앱 카드와 섞여 배치되기 다소 애매함.

---

## 3. 상단 탭 바 변화 비교

통합 시 상단 내비게이션 바는 다음과 같이 극도로 정돈됩니다:

```text
[현재 탭 바 - 총 8개 그룹]
[🖥️ 시스템] [⚡ 빠른 실행] [📁 파일/폴더] [🔢 생성기] [🧪 JS실행기] [📝 메모] [💼 업무 & 협업 ▾] [📊 뷰어 / 다이어그램 ▾]

[통합 후 탭 바 - 총 7개 그룹 (최적화 🌟)]
[🖥️ 시스템] [⚡ 빠른 실행] [🔢 생성기] [🧪 JS실행기] [📝 메모] [💼 업무 & 협업 ▾] [📊 뷰어 / 다이어그램 ▾]
```
- 중복되던 `📁 파일/폴더` 탭이 `⚡ 빠른 실행`으로 자연스럽게 흡수 통합됩니다.

---

## 4. 백엔드 및 데이터 호환성 (Zero Data Loss)

- **데이터베이스 유지**:
  - SQLite의 `quick_launch` 테이블과 `shortcuts` 테이블은 그대로 유지하므로, 기존 사용자가 등록해 둔 데이터는 100% 보존됩니다.
- **서비스 계층 유지**:
  - `quick_launch_service.py`와 `shortcuts_service.py`의 고유 실행 로직(프로세스 실행, 터미널 실행, VS Code 실행 등)을 그대로 보존하여 안정성 확보.
- **백업 및 AI 검색 호환**:
  - `backup_service.py`와 `ai_search_service.py`는 이미 두 테이블을 모두 지원하므로 추가 마이그레이션 불필요.

---

## 5. 단계별 구현 계획 (Proposed Implementation Steps)

1. **마크업 통합 (`web/index.html`)**:
   - 상단 탭 바에서 `data-tab="files"` 버튼 제거.
   - `#tab-launch` 섹션 내부를 상단 '📁 폴더 바로가기 & 터미널 런처' + 하단 '⚡ 프로그램 빠른 실행' 2단 구성으로 통합.
   - 기존의 `#tab-files` 섹션 코드를 `#tab-launch` 내부로 자연스럽게 병합.
2. **스크립트 통합 및 온디맨드 로드 (`web/js/app.js`)**:
   - `switchTab('launch')` 진입 시 `loadFolderShortcuts()`와 `loadQuickLaunchItems()`를 함께 로드하도록 바인딩.
3. **스타일 정돈 (`web/style.css`)**:
   - `#tab-launch` 내 2개 섹션 간의 여백(Margin), 섹션 구분선(Subtle divider) 및 터미널 런처 툴바 레이아웃 정돈.
4. **검증 및 배포**:
   - 구문 검사 및 회귀 테스트 통과 후 Git 커밋 & 푸시.
