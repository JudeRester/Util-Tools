# 🤖 Antigravity CLI(agy) UI/UX 전면 개선 계획서

본 문서는 사용자가 제공한 2장의 화면 캡처(`D:\Temp\찌그러지고 직관성 떨어짐.png`, `D:\Temp\목록형일 필요가 있고 제목_프로젝트_세션아이디가 나와야하니 테이블이 무난할듯.png`) 피드백을 바탕으로, **설정 UI 찌그러짐 해결** 및 **세션 목록의 테이블(Table) 뷰 전면 개편**을 진행하기 위한 상세 설계 계획서입니다.

---

## 1. 문제 분석 및 사용자 피드백 반영

### 1-1. `tab-system`: 설정 카드의 레이아웃 왜곡 (찌그러짐)
- **원인**: 220px 너비의 툴 카드 그리드(`.button-grid`) 안에 텍스트와 토글, 배지를 억지로 집어넣어, 제목(`Antigravit...`)과 설명이 잘리고 `감/지/됨` 배지가 세로로 찌그러짐.
- **개선안**:
  - 시스템 도구 그리드와 분리된 **독립적인 와이드 설정 패널(`.settings-panel-row`)**로 전면 리팩토링.
  - 좌측에 아이콘 + 타이틀(`Antigravity CLI 연동`) + 감지 배지 + 상세 설명이 한 줄 또는 넉넉한 공간에 표시되고, 우측에 큼직하고 명확한 토글 스위치 배치.

### 1-2. `tab-launch`: agy 세션 카드의 정보 가독성 부재
- **원인**: 세션 목록을 220px 좁은 그리드 카드로 나열하여 제목(`Curre...`, `Full Pr...`), 프로젝트 경로, 세션 ID, 버튼 라벨(`⚡ 터미널 열`)까지 모조리 잘려나가 직관성이 완전히 상실됨.
- **개선안**:
  - 사용자 피드백대로 **"목록형 테이블(Table) 뷰"**로 전면 전환.
  - **테이블 컬럼**:
    1. **세션 제목 (`Title`)**: 주요 작업 내용 전체 표시 (긴 경우 툴팁 제공)
    2. **프로젝트 / 워크스페이스 (`Project`)**: 폴더명 뱃지 + 전체 경로 표시 (현재 프로젝트 세션은 `📍 현재 프로젝트` 하이라이트)
    3. **세션 ID (`Session ID`)**: 8자리 단축 ID 표기 (클릭 시 전체 UUID 클립보드 복사)
    4. **스텝 수 (`Steps`)**: 대화 턴 수 표기 (예: `399 스텝`)
    5. **마지막 작업 일시 (`Updated`)**: `2026-08-25 01:55` 형태
    6. **작업 (`Action`)**: 우측 정렬된 `[⚡ 터미널 열기]` 버튼

---

## 2. 세부 UI 설계안

### 2-1. `tab-system`: 와이드 설정 패널 UI
```text
┌───────────────────────────────────────────────────────────────────────────────────────────────────┐
│ ⚙️ 도구 및 외부 환경 설정                                                                          │
│ ┌───────────────────────────────────────────────────────────────────────────────────────────────┐ │
│ │ 🤖  Antigravity CLI (agy) 세션 연동   [🟢 감지됨]                                  [  ON / OFF  ] │ │
│ │     로컬 agy CLI 세션 목록을 조회하고 작업 폴더에서 즉시 터미널을 이어 실행합니다.                    │ │
│ └───────────────────────────────────────────────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────────────────────────────────────────────┘
```

### 2-2. `tab-launch`: 목록형 세션 테이블 UI
```text
┌───────────────────────────────────────────────────────────────────────────────────────────────────┐
│ 🤖 Antigravity CLI 세션 목록 (총 24개)                     [📍 현재 프로젝트] [🌐 전체 세션]  [🔄] │
│ 🔍 세션 제목, 프로젝트, 세션 ID 실시간 검색...                                                     │
├───────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 제목                          │ 프로젝트              │ 세션 ID   │ 스텝   │ 마지막 작업      │ 실행 │
├───────────────────────────────┼───────────────────────┼───────────┼────────┼─────────────────┼──────┤
│ Current Project Status Recap  │ 📍 python (D:\python) │ cfb67627  │ 399 스텝 │ 08-25 01:55     │ [⚡] │
│ Full Project Code Inspection  │ 📍 python (d:\python) │ 3dffceb6  │ 228 스텝 │ 08-24 06:10     │ [⚡] │
│ SF Query Usage Tracking       │ 📁 server-setup       │ e16ff8b1  │ 339 스텝 │ 08-21 04:52     │ [⚡] │
│ Workspace Program Function... │ 📁 logParser          │ 4a10f28a  │ 48 스텝  │ 08-20 04:38     │ [⚡] │
└───────────────────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 3. 수정 및 구현 대상 파일

1. **`web/index.html`**:
   - `tab-system`: 찌그러진 `.tool-btn`을 와이드 `.settings-panel-row` 구조로 교체.
   - `tab-launch`: `#agy-sessions-grid`를 `#agy-sessions-table-container`로 변경하고 실시간 검색창 필드 추가.
2. **`web/js/agy_sessions.js`**:
   - 카드 생성 HTML을 테이블 행(`<tr>...</tr>`) 렌더링으로 전면 리팩토링.
   - 실시간 세션 검색 기능(`filterByKeyword(keyword)`) 추가.
   - 세션 ID 클릭 시 클립보드 복사(`navigator.clipboard.writeText`) 기능 추가.
3. **`web/style.css`**:
   - `.settings-panel-row` 스타일 (반응형 패딩, 배지 인라인 배치, 토글 정돈).
   - `.agy-table` 스타일 (다크 테마 테이블, 호버 하이라이트, 스텝 뱃지, 프로젝트 뱃지, 컬럼 너비 최적화).

---

## 4. 검증 계획

1. **시각적 정합성 검증**:
   - `tab-system`의 설정 카드가 더 이상 찌그러지지 않고, `감지됨` 배지와 텍스트가 시원하게 노출되는지 확인.
   - `tab-launch`의 세션 목록이 깔끔한 테이블로 렌더링되어 제목, 프로젝트, 세션 ID, 스텝 수가 잘림 없이 표시되는지 확인.
2. **기능 검증**:
   - `[현재 프로젝트]` / `[전체 세션]` 필터 동작 확인.
   - 실시간 검색창에 키워드 입력 시 즉시 필터링되는지 확인.
   - `[⚡ 터미널 열기]` 클릭 시 해당 세션 작업 디렉토리에서 정상 실행되는지 확인.
3. **코드 무결성 파이프라인**:
   - `node -c web/js/*.js`
   - CSS brace count 검사
   - 백엔드 회귀 테스트
